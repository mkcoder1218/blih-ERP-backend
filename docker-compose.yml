
services:
  migrate:
    build:
      context: .
      target: runner
    image: blih-erp-backend:latest
    command: ["npm", "run", "db:migrate"]
    environment:
      NODE_ENV: production
      DB_HOST: db
      DB_PORT: 5432
    env_file:
      - .env.production
    depends_on:
      db:
        condition: service_healthy
    restart: "no"

  app:
    build:
      context: .
      target: runner
    image: blih-erp-backend:latest
    restart: unless-stopped
    ports:
      - "${APP_PORT:-4000}:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      DB_HOST: db
      DB_PORT: 5432
      DB_SYNC: "false"
      API_VERSION: ${API_VERSION:-v1}
    env_file:
      - .env.production
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:' + (process.env.PORT || 4000) + '/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  db:
    image: postgres:15-alpine
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      POSTGRES_USER: ${DB_USER:?Set DB_USER by running docker compose --env-file .env.production ...}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD by running docker compose --env-file .env.production ...}
      POSTGRES_DB: ${DB_NAME:?Set DB_NAME by running docker compose --env-file .env.production ...}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
  uploads_data:
